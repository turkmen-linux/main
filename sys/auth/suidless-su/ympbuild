#!/usr/bin/env bash
name='suidless-su'
release='1'
version='1.0.0'
url='https://gitlab.com/turkmen-linux/devel/sources/suidless-su'
description='su command replacement. No suid bit required.'
email='aliriza.keskin@pardus.org.tr'
maintainer='aliriza'
license=('GPLv3')
source=("https://gitlab.com/turkmen-linux/devel/sources/suidless-su/-/archive/$version/suidless-su-$version.tar.gz")
depends=()
makedepends=()
sha256sums=('17623cb0dc18d5de0b40b8e7818b451326166f1887f01044285b30a4f683593d')
group=(sys.auth)
uses=()
arch=('x86_64')

cd $name-$version

setup () {
    meson setup build \
      --prefix=/usr
}

build () {
    ninja -C build $jobs
}

package () {
    ninja -C build install $jobs
}

